// JavaScript for additional features like sound or interaction (optional)
document.addEventListener('DOMContentLoaded', () => {
    const message = document.querySelector('#birthday-message');
    
    // Add a small interactive effect when the user clicks the message
    message.addEventListener('click', () => {
        alert('Thank you for visiting, Priyanka! 🎉');
    });
});
